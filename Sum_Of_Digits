#Program To Find Sum Of Digits of an number
Example:
       Input:     N=1234
       output:   1+2+3+4=10

int main()
{
   int n,digit,sum=0;
   printf("Enter the number");
   scanf("%d",&n);           
   while(n!=0)
   {
       digit=n%10;          
       sum=sum+digit;    
       n=n/10;
    }
   printf("Sum is %d",sum);
   return 0;
} 
